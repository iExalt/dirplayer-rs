diff --git a/core/src/avm1/globals/local_connection.rs b/core/src/avm1/globals/local_connection.rs
index 9fc65d3c6..395854ca6 100644
--- a/core/src/avm1/globals/local_connection.rs
+++ b/core/src/avm1/globals/local_connection.rs
@@ -23,13 +23,17 @@ use std::cell::RefCell;
 // unaffected; dirplayer only acts on connection names it has registered. The
 // `dirplayer_`-prefixed global keeps us from colliding with stock Ruffle, and
 // `catch` means a page without dirplayer (missing global) is a silent no-op.
+#[cfg(target_arch = "wasm32")]
 use js_sys::JSON;
+#[cfg(target_arch = "wasm32")]
 use wasm_bindgen::prelude::*;
 
+#[cfg(target_arch = "wasm32")]
 #[wasm_bindgen]
 extern "C" {
-    #[wasm_bindgen(js_name = "dirplayer_localConnectionSend", catch)]
-    fn dirplayer_local_connection_send(
+    #[wasm_bindgen(js_name = "dirplayer_localConnectionSendOwned", catch)]
+    fn dirplayer_local_connection_send_owned(
+        owner_key: String,
         connection_name: String,
         method_name: String,
         args_json: String,
@@ -251,8 +255,12 @@ pub fn send<'gc>(
     }
 
     // dirplayer fork: hand the send off to dirplayer as well (see extern above).
-    // Serialize the trailing args as a plain JSON array of their values.
-    {
+    // Serialize the trailing args as a plain JSON array of their values. Keep
+    // the entire host conversion behind the wasm/owner boundary: native
+    // players must not touch the JS conversion store before normal AMF
+    // delivery, and stale/no-owner players have no host capability.
+    #[cfg(target_arch = "wasm32")]
+    if let Some(owner_key) = activation.context.dirplayer_owner_key.clone() {
         let arr = js_sys::Array::new();
         for arg in &args[2..] {
             let ext_val = activation.store_and_convert_for_lingo(*arg);
@@ -263,7 +271,8 @@ pub fn send<'gc>(
             .ok()
             .and_then(|s| s.as_string())
             .unwrap_or_else(|| "[]".to_string());
-        let _ = dirplayer_local_connection_send(
+        let _ = dirplayer_local_connection_send_owned(
+            owner_key,
             connection_name.to_utf8_lossy().into_owned(),
             method_name.to_utf8_lossy().into_owned(),
             args_json,
diff --git a/core/src/context.rs b/core/src/context.rs
index d7be0644f..cab886e28 100644
--- a/core/src/context.rs
+++ b/core/src/context.rs
@@ -139,6 +139,10 @@ pub struct UpdateContext<'gc> {
     /// that the player lives across I/O boundaries.
     pub player: Weak<Mutex<Player>>,
 
+    /// Generation-bound owner identity for the dirplayer web bridge. Native
+    /// players leave this unset and therefore never call the JS bridge.
+    pub dirplayer_owner_key: Option<String>,
+
     /// The player's load manager.
     ///
     /// This is required for asynchronous behavior, such as fetching data from
diff --git a/core/src/player.rs b/core/src/player.rs
index 19105d171..edd8119d6 100644
--- a/core/src/player.rs
+++ b/core/src/player.rs
@@ -374,6 +374,10 @@ pub struct Player {
     /// player lives across `await` calls in async code.
     self_reference: Weak<Mutex<Self>>,
 
+    /// Generation-bound owner identity used only by the dirplayer web bridge.
+    /// Native backends leave this unset.
+    dirplayer_owner_key: Option<String>,
+
     /// The current frame of the main timeline, if available.
     /// The first frame is frame 1.
     current_frame: Option<u16>,
@@ -2328,6 +2332,7 @@ impl Player {
                 mouse_position: &this.mouse_position,
                 drag_object,
                 player: this.self_reference.clone(),
+                dirplayer_owner_key: this.dirplayer_owner_key.clone(),
                 load_manager,
                 system: &mut this.system,
                 page_url: &mut this.page_url,
@@ -3088,6 +3093,7 @@ pub struct PlayerBuilder {
     frame_rate: Option<f64>,
     external_interface_provider: Option<Box<dyn ExternalInterfaceProvider>>,
     fs_command_provider: Box<dyn FsCommandProvider>,
+    dirplayer_owner_key: Option<String>,
     #[cfg(feature = "known_stubs")]
     stub_report_output: Option<std::path::PathBuf>,
     avm2_optimizer_enabled: bool,
@@ -3144,6 +3150,7 @@ impl PlayerBuilder {
             frame_rate: None,
             external_interface_provider: None,
             fs_command_provider: Box::new(NullFsCommandProvider),
+            dirplayer_owner_key: None,
             #[cfg(feature = "known_stubs")]
             stub_report_output: None,
             avm2_optimizer_enabled: true,
@@ -3352,6 +3359,14 @@ impl PlayerBuilder {
         self
     }
 
+    /// Sets the generation-bound owner identity used by the dirplayer web
+    /// bridge. Native callers leave this unset and retain normal Ruffle
+    /// LocalConnection behavior.
+    pub fn with_dirplayer_owner_key(mut self, owner_key: Option<String>) -> Self {
+        self.dirplayer_owner_key = owner_key;
+        self
+    }
+
     pub fn with_gamepad_button_mapping(mut self, mapping: HashMap<GamepadButton, KeyCode>) -> Self {
         self.gamepad_button_mapping = mapping;
         self
@@ -3526,6 +3541,7 @@ impl PlayerBuilder {
                 },
                 needs_render: true,
                 self_reference: self_ref.clone(),
+                dirplayer_owner_key: self.dirplayer_owner_key.clone(),
                 load_behavior: self.load_behavior,
                 spoofed_url: self.spoofed_url.clone(),
                 compatibility_rules: self.compatibility_rules.clone(),
diff --git a/web/packages/core/src/internal/player/inner.tsx b/web/packages/core/src/internal/player/inner.tsx
index 063bdc2e3..c507747db 100644
--- a/web/packages/core/src/internal/player/inner.tsx
+++ b/web/packages/core/src/internal/player/inner.tsx
@@ -192,6 +192,7 @@ export class InnerPlayer {
 
     private swfUrl?: URL;
     private instance: RuffleHandle | null;
+    private dirplayerOwnerKey: string | null = null;
     private newZipWriter: (() => ZipWriter) | null;
     private lastActivePlayingState: boolean;
 
@@ -400,6 +401,14 @@ export class InnerPlayer {
         this.addFSCommandHandler(handler);
     }
 
+    /** Bind this element to one immutable dirplayer owner generation. */
+    dirplayer_set_owner_key(ownerKey: string): void {
+        if (this.dirplayerOwnerKey !== null && this.dirplayerOwnerKey !== ownerKey) {
+            throw new Error("dirplayer owner key cannot be rebound");
+        }
+        this.dirplayerOwnerKey = ownerKey;
+    }
+
     /**
      * dirplayer fork: directly inject a synthetic mouse event into the
      * underlying Ruffle WASM core, bypassing the canvas's DOM event
@@ -758,6 +767,9 @@ export class InnerPlayer {
         });
         this.newZipWriter = zipWriterClass;
         configureBuilder(builder, this.loadedConfig || {});
+        if (this.dirplayerOwnerKey !== null) {
+            builder.setDirplayerOwnerKey(this.dirplayerOwnerKey);
+        }
         builder.setVolume(this.volumeSettings.get_volume());
 
         if (this.loadedConfig?.fontSources) {
diff --git a/web/packages/core/src/internal/player/ruffle-player-element.tsx b/web/packages/core/src/internal/player/ruffle-player-element.tsx
index 226018025..afa16b1f1 100644
--- a/web/packages/core/src/internal/player/ruffle-player-element.tsx
+++ b/web/packages/core/src/internal/player/ruffle-player-element.tsx
@@ -86,6 +86,11 @@ export class RufflePlayerElement extends HTMLElement implements PlayerElement {
         this.#inner.dirplayer_addFSCommandHandler(handler);
     }
 
+    /** Bind the player element to an immutable dirplayer owner generation. */
+    dirplayer_set_owner_key(ownerKey: string): void {
+        this.#inner.dirplayer_set_owner_key(ownerKey);
+    }
+
     /**
      * dirplayer fork: forward `dirplayer_dispatchPointer` to the inner
      * player so flashPlayerManager.ts can inject Director-side clicks
diff --git a/web/packages/core/tools/build_wasm.ts b/web/packages/core/tools/build_wasm.ts
index 777ebd894..2f9f74ff4 100644
--- a/web/packages/core/tools/build_wasm.ts
+++ b/web/packages/core/tools/build_wasm.ts
@@ -1,5 +1,6 @@
 import { execFileSync } from "child_process";
 import { copyFileSync, mkdirSync, rmSync } from "fs";
+import { isAbsolute, join, resolve } from "path";
 import * as process from "process";
 
 function runWasmOpt({ path, flags }: { path: string; flags?: string[] }) {
@@ -118,7 +119,18 @@ function buildWasm(
             rustFlags,
             extensions,
         });
-        originalWasmPath = `../../../target/wasm32-unknown-unknown/${profile}/ruffle_web.wasm`;
+        const configuredTargetDir = process.env["CARGO_TARGET_DIR"];
+        const targetDir = configuredTargetDir
+            ? isAbsolute(configuredTargetDir)
+                ? configuredTargetDir
+                : resolve(process.cwd(), configuredTargetDir)
+            : resolve(process.cwd(), "../../../target");
+        originalWasmPath = join(
+            targetDir,
+            "wasm32-unknown-unknown",
+            profile,
+            "ruffle_web.wasm",
+        );
         if (wasmSource === "cargo_and_store") {
             copyFileSync(originalWasmPath, `../../dist/${filename}.wasm`);
         }
diff --git a/web/src/builder.rs b/web/src/builder.rs
index 1f7909002..558daec75 100644
--- a/web/src/builder.rs
+++ b/web/src/builder.rs
@@ -66,6 +66,7 @@ pub struct RuffleInstanceBuilder {
     pub(crate) url_rewrite_rules: Vec<(RegExp, String)>,
     pub(crate) scrolling_behavior: ScrollingBehavior,
     pub(crate) device_font_renderer: DeviceFontRenderer,
+    pub(crate) dirplayer_owner_key: Option<String>,
 }
 
 impl Default for RuffleInstanceBuilder {
@@ -106,6 +107,7 @@ impl Default for RuffleInstanceBuilder {
             url_rewrite_rules: vec![],
             scrolling_behavior: ScrollingBehavior::Smart,
             device_font_renderer: DeviceFontRenderer::Embedded,
+            dirplayer_owner_key: None,
         }
     }
 }
@@ -358,6 +360,11 @@ impl RuffleInstanceBuilder {
         };
     }
 
+    #[wasm_bindgen(js_name = "setDirplayerOwnerKey")]
+    pub fn set_dirplayer_owner_key(&mut self, owner_key: String) {
+        self.dirplayer_owner_key = Some(owner_key);
+    }
+
     // TODO: This should be split into two methods that either load url or load data
     // Right now, that's done immediately afterwards in TS
     pub fn build(&self, parent: HtmlElement, js_player: JavascriptPlayer) -> Promise {
@@ -712,6 +719,7 @@ impl RuffleInstanceBuilder {
             .with_align(self.stage_align, self.force_align)
             .with_scale_mode(self.scale, self.force_scale)
             .with_frame_rate(self.frame_rate)
+            .with_dirplayer_owner_key(self.dirplayer_owner_key.clone())
             .with_page_url(window.location().href().ok())
             .with_gamepad_button_mapping(self.gamepad_button_mapping.clone())
             .build();
